# 凭据文件（含真实账号与密码哈希）——切勿提交
dsh-webui-auth.json

# 审计日志（含用户名/IP/UA 等安全事件）——切勿提交
audit.jsonl

# 通用忽略
*.log
.DS_Store
Thumbs.db

# 持久化会话（活动会话令牌）与首启初始化令牌——切勿提交。
# 上游 .gitignore 未覆盖它们：npm/tarball 安装时数据落在 $DSH_HOME，
# 但本地 link 安装时就落在本目录（见 README「数据文件位置」）。
sessions.jsonl
setup-token
audit-hmac-key
